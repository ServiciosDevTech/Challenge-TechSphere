# Frontend — PostOp Care

UI React + Vite + TypeScript.

Arranque y documentación completa: ver el [README raíz](../README.md).

```powershell
npm install
npm run dev
```

Rutas: `/call` · `/admin` · `/historial`.
